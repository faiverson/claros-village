export enum Variant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ACCENT = 'accent',
  ERROR = 'destructive',
}

export enum Amenity {
  Sum = 'sum',
  Gym = 'gym',
  Soccer = 'soccer',
}

export enum SumShift {
  Day = 'day',
  Night = 'night',
  Both = 'both',
}

export enum SumRoom {
  Big = 'big',
  Small = 'small',
}
